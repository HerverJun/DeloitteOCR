# 总纲剩余任务与最终验收矩阵

目标保持为《开发总纲》的完整首版。第二、三周交付是已有基础，不将其范围当作最终验收范围。仅当下表各项均有对应范围的实际证据时，才确认最终完成并关机。

| 要求 | 当前权威证据 | 剩余工作与完成证据 |
|---|---|---|
| 四引擎、本机 GUI、项目保存、取消恢复、图像校正、编辑与导出 | 新版完整应用 48 项、核心 GUI 13 项、新增 GUI 5 项与下载独立读回通过 | 真实四引擎、强退恢复、原图与编辑保留均有证据；系统禁网另见交付回执 |
| 一次设置批次模型与预处理预设 | 持久化预处理与不可变版本已实现；500 张批次及新版 GUI 两图批次通过 | 任务固定输入版本、处理参数、引擎包 ID；不改原图 |
| 数百图片与有限 CPU 并发 | 新版 500 张不同合成图全部完成，126.64 张/分钟；预热后进程树增长 5.67 MiB、service 5.56 MiB；逐项结果与长编号、损坏/超大拒绝和退出通过 | 原始采样与窗口指标保留；这是工程负载，不是准确率测试集 |
| 20 次连续加载卸载 | 新版四引擎各 5 次，20/20 通过；子进程全部退出，卸载后设备占用 1008–1116 MiB，回归桌面基线 | 新旧原始采样分别保留；新版图表在 stability-report-final |
| OOM 明确失败和可重试，不降低质量 | 已分类 CUDA/OOM 错误，失败保留且释放引擎；真实空白失败、取消重试与崩溃恢复通过 | 专门错误分类、异常资源回收与恢复测试，真实硬件测量 |
| 项目占用与清理 | 单元测试与真实 GUI 确名删除、保留另一项目均通过 | 最终打包保留对应证据 |
| 完整离线引擎包导入、校验、暂存、切换和回退 | 4.34 GB 的实际 PP-OCR 完整包经 API 和 GUI 激活、识别、回退验证通过；重启保留及旧任务版本快照通过 | 最终打包保留证据；原子文件和模态对话框可访问性缺陷已修复并复验 |
| 启动自检 GPU/驱动/显存/磁盘/模型完整性/依赖加载 | 新版实际通过 65,000 文件完整检查和原生运行时加载；健康接口版本为 0.7.0rc1；Windows 文件占用缺陷已修复 | 本机全量检查、变更文件/缺依赖测试均保留；独立机器仍待实测 |
| 表格专项：无边框、合并、多表、空格、长编号、手写、透视 | 公开 40 表格及另 12 项工程推理/导出通过；三引擎对真实整页 3 表、43 单元格均全对；合成双表均输出 2 工作表 | 混元在合成双表/逆透视图把合并标题变为 caption，保留结构误差；真实手机照片仍需内网复核 |
| 至少 200 张标注图片评测 | 首轮与新版各四引擎 × 200 张，均已完成；78 手写、82 印刷、40 表格；指标、33 项失败样例图册和哈希完整 | 新版 796 项成功原始输出与基线零差异，4 次失败一致；公开区域评测不能替代内网复核 |
| 与官方推理路线保真对照 | 最新缓存版本四引擎各文字/表格两样例，独立上游 API 全原始输出零差异；浮点容差 1e-5 | 绑定冻结清单，最终包再比较实际执行组件字节；不扩大为所有图片准确率保证 |
| PaddleOCR-VL 显存管理 | 最新 200 张已完成：设备峰值 10759 MiB，吞吐 22.39 张/分钟；基线为 15902 MiB、15.16 张/分钟。198 张成功原始输出零差异，两项失败/指标一致 | 继续完整四引擎报告与目标 A4000 验证；设备总占用含桌面，0.5 秒采样不保证捕获瞬时峰值 |
| 内网真实样本复核 | 未提供 | 用户提供真实样本/标注及复核结果 |
| 另一台无开发环境、禁止联网 Windows | 当前机器是开发机 | 独立机器实测及可验证机器/环境/禁网证据；沙箱或本机搬迁不能冒充 |
| Xeon W5-2455X / 64 GB / RTX A4000 16 GB | 当前为 RTX 4070 Ti SUPER | 目标机器记录冷启动、单张、吞吐、峰值显存和约 2 GB 余量 |
| 最终 ZIP、源码、锁、许可、样例、说明和报告 | 本机候选版实现与测试已收口，历史交付保留；632 项原始许可/notice 已索引 | 本文为打包前快照；0.7.0rc1 的归档/源码/组件/搬迁/禁网最终状态以 release 目录的独立 JSON 回执为准 |

本机可完成的开发与测量持续推进。需要外部设备、真实数据或用户标注的门槛保留为未通过，不能用验收工具已写好来替代实际验收。

本机详细证据位于 `E:\OCR-final-build`，新版目录均明确带 `-final`（多表为 `table-scenarios`）。分发证据清单位于 `release/evidence/collection.json`，逐项列出未通过门槛，不能仅因文件收集成功就判断发布完成。外部机器步骤见《外部机器验收交接》。公开区域评测不替代完整拍照场景及内网业务字段复核。
